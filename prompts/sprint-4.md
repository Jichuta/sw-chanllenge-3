# Prompt Log — Sprint 4: Prompt Log, Final Polish, and Delivery Completion

## Prompt 4.1: Create prompt logs and verify acceptance criteria

**Prompt:**
```
Create the /prompts folder with prompt-log files for each sprint (1-4).
Each file documents the prompts used, assistant responses, and any manual edits.

Then verify all functional and technical acceptance criteria from challenge3.md:
- The public registration form captures all required fields.
- Data is persisted in a database through an API.
- Admin panel shows candidate list with filters by location and English level.
- Traffic-light rule highlights B2+ green, B1 yellow, lower levels red.
- The form is responsive (mobile and desktop).
- The PDF CV can be opened from the admin table.
- The candidate's profile can be marked as accepted, rejected, and in review.
- Spec documents exist before code.
- A /prompts folder collects the prompts used per feature.
- A planning file tracks progress (todo / doing / done).
- Clear separation of concerns (API, persistence, UI).
- Basic input validation, error handling, CV file-type/size checks.
- README lets a peer run the project locally.

Run full verification: pnpm typecheck, pnpm lint, pnpm test, pnpm build.
Update README with final delivery notes.
```

**Assistant response:**
Created all four prompt log files in `/prompts/`. Verified all acceptance criteria pass. Full verification suite passes. README updated.

**Manual edits:** None.
